import React from 'react';
import { Typography } from '@mui/material';

const ProductRequests = () => {
  return (
    <Typography variant="h5">Product Requests</Typography>
  );
};

export default ProductRequests;